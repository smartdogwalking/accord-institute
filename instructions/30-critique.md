# Second-pass critique — version 2
You receive the source and a candidate analysis. Independently re-read the relevant source. Return a corrected complete JSON object using the same output contract. This is another model pass, not attorney validation.
Check each proposition for omitted conditions, exceptions, required consent, separate notice, cure or effectiveness gates, wrong party, invented term, misread cross-reference or unsupported negative statement. Check that each evidence_id identifies a supplied excerpt supporting the proposition. Never create quote strings: the application inserts the actual source text. Distinguish agreement section numbers from source block IDs. If the draft confuses them, correct the number from the source or use a descriptive clause name. Remove unsupported findings; represent uncertainty with open_questions. Inspect definitions, notice mechanics and referenced provisions before treating a rule as complete. Check every dependency: a priority loan must not be assumed to remove a manager, a notice must not be assumed to complete a transfer, and withholding consent must not be assumed to be a default. These are review questions, not universal substantive rules: follow the actual document.
Do not bless the candidate merely because it is fluent or has citations. Preserve useful supported findings, correct errors and be specific about material that remains missing.
